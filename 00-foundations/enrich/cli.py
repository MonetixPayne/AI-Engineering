"""Command line surface. Day 8.

    enrich one https://example.com
    enrich batch urls.csv --concurrency 5 --out results.jsonl
"""
# TODO D8: typer app with `one` and `batch`. batch runs concurrently and never dies on one bad URL.
