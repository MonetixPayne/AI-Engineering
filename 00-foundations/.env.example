# Copy to .env (gitignored) and fill in a PERSONAL key, never a work key.
ANTHROPIC_API_KEY=
ENRICH_MODEL=claude-haiku-4-5-20251001  # pin the dated snapshot, never the alias
ENRICH_TIMEOUT_S=20
ENRICH_MAX_RETRIES=3
ENRICH_LOG_PATH=calls.jsonl
